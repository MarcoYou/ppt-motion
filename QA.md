# 검증 범위

현재 엔진 버전: **0.3.0**.

2026-09-24: 저장소에 포함된 파일만 별도 폴더로 복사하고, 새 Python 3.14 가상환경에 필수 의존성을 설치한 뒤 **37개 테스트 통과**를 확인했습니다.

## 자동화 테스트

```sh
python -m unittest discover -s tests -v
```

테스트는 임시 폴더에서 PDF/PPTX를 생성하므로 개인 발표자료나 PowerPoint 설치가 필요하지 않습니다.

- 도형 좌표·칠하기 순서·페이지 크기 보존
- 양수·음수 막대의 실제 영점, 원본 점선 경로, 래스터 이미지 보존
- 원형 채우기의 중심·반경·부분 각도·역방향 및 좌표 변환 검증
- 잘못된 효과·좌표·색상·요소 선택 거부
- 원본·추출·설정·출력 해시 검증 및 오래된 계획 거부
- 빌드 실패 시 기존 출력 보존, 동일 입력의 재현성
- PPTX 슬라이드 순서·숨김·중첩 그룹·표 병합·차트 캐시·외부 링크 처리
- 객체 계획의 저장·재생성·적용과 수동 효과 보존

GitHub Actions는 Python 3.11과 3.13에서 이 테스트를 실행하도록 구성되어 있습니다. 실제 통과 여부는 Actions 결과를 확인하세요.

## 브라우저 검수

개발 중 로컬 뷰어에서 다음 동작을 확인했습니다.

- 양수·음수 막대 성장과 누적 구간의 연결 유지
- 데이터 경로의 진행, 점선 보존, 히트맵 색 표시
- 도넛의 회색 트랙 위 색 채우기, 원래 무늬·중심 숫자 유지
- 재생 완료·중단·효과 끄기에서 임시 트랙과 적용 마스크 제거
- 재생·이동·원본 비교·요소 선택
- 전체화면에서 외부 발표 설명 숨김
- 동작 줄이기 설정에서 최종 정지 화면 표시

## 검증의 한계

구조 검사는 모든 PDF와 브라우저에서의 완벽한 시각적 동일성을 보장하지 않습니다. 새 자료마다 시작·중간·완료 프레임을 원본과 비교해야 합니다. 통계·주장의 사실 검증, PowerPoint 네이티브 애니메이션 출력, 외부 호스팅 배포는 이 검사에 포함되지 않습니다.
