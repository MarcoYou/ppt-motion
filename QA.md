# 검증 범위

현재 엔진 버전: **0.5.0**.

2026-09-24: Python 3.14에서 **77개 테스트 통과**를 확인했습니다. 이전 0.3.0의 별도 체크아웃·새 가상환경 검사에 설치·런타임·배포 ZIP·단일 HTML 검사를 추가했습니다.

## 자동화 테스트

```sh
python -m unittest discover -s tests -v
```

테스트는 임시 폴더에서 PDF/PPTX를 생성하므로 개인 발표자료나 PowerPoint 설치가 필요하지 않습니다.

- 도형 좌표·칠하기 순서·페이지 크기 보존
- 양수·음수 막대의 실제 영점, 원본 점선 경로, 래스터 이미지 보존
- 원형 채우기의 중심·반경·부분 각도·역방향 및 좌표 변환 검증
- 잘못된 효과·좌표·색상·요소 선택 거부
- 원본·추출·설정·출력 해시 검증 및 오래된 계획 거부
- 빌드 실패 시 기존 출력 보존, 동일 입력의 재현성
- PPTX 슬라이드 순서·숨김·중첩 그룹·표 병합·차트 캐시·외부 링크 처리
- 객체 계획의 저장·재생성·적용과 수동 효과 보존
- 설치 파일의 완전성·반복 설치·수정본 감지·기존 설치 백업·실패 시 복원
- 런타임 캐시 재사용·정확한 패키지 버전·설치 실패 재시도·전역 환경 오염 방지
- 네 ZIP의 재현성·체크섬·버전 일치·개인자료 제외·압축 해제 후 설치
- 단일 HTML의 원본 SVG/PNG 일치·HTML 문자 이스케이프·오래된 빌드 및 덮어쓰기 거부

GitHub Actions는 Python 3.11과 3.13에서 이 테스트를 실행하도록 구성되어 있습니다. 실제 통과 여부는 Actions 결과를 확인하세요.

OpenAI 제출 ZIP의 portable manifest는 공식 Agent Plugins 1.0.0 스키마, 호환 manifest는 Plugin Creator 검사기, 포함 스킬은 Skill Creator 검사기를 통과했습니다. 제출 ZIP은 표준 `skills/` 구조이며 저장소는 기존 `skill/` 소스를 유지합니다.

## 클라이언트 설치 검수

- Codex와 Claude Code용 ZIP을 각각 임시 폴더에 풀어 별도 스킬 경로에 설치했습니다. 0.4.0에서는 실제 사용자 스킬 설정을 변경하지 않고 검사했습니다.
- Claude Code 2.1.281에서 플러그인과 마켓플레이스의 엄격한 명세 검사를 통과했습니다. 임시 `CLAUDE_CONFIG_DIR`에서 실제 마켓플레이스 추가·설치 후 스킬 1개를 인식하고 버전 0.4.0이 활성화되는 것을 확인했습니다. 설치 캐시도 배포 파일과 일치했습니다.
- Desktop ZIP만 압축 해제한 별도 환경에서 새 런타임을 준비하고, 가상 PDF의 첫 1쪽에 양수·음수 막대와 선 효과를 적용해 `init → plan → apply-plan → build → check → export-html` 전체 흐름을 통과했습니다.
- Desktop ZIP은 Anthropic의 사용자 지정 스킬 형식으로 구성합니다. 실제 Claude Desktop 계정에 업로드하거나 유료 대화를 실행한 검증은 포함하지 않습니다. 코드 실행·의존성 다운로드 가능 여부는 해당 환경에 따릅니다.

## 브라우저 검수

개발 중 로컬 뷰어에서 다음 동작을 확인했습니다.

- 양수·음수 막대 성장과 누적 구간의 연결 유지
- 데이터 경로의 진행, 점선 보존, 히트맵 색 표시
- 도넛의 회색 트랙 위 색 채우기, 원래 무늬·중심 숫자 유지
- 재생 완료·중단·효과 끄기에서 임시 트랙과 적용 마스크 제거
- 재생·이동·원본 비교·요소 선택
- 전체화면에서 외부 발표 설명 숨김
- 동작 줄이기 설정에서 최종 정지 화면 표시

0.4.0에서는 가상 데이터 2쪽을 단일 HTML로 내보내고, 이 파일만 제공하는 HTTP 경로에서 확인했습니다. 슬라이드/SVG/PNG/CSS/JS의 별도 요청 없이 재생·이동·원본 비교·효과 끄기·전체화면 캡션 숨김이 동작했습니다. 브라우저 콘솔 경고·오류는 없었습니다. 모든 운영체제의 `file://` 실행을 각각 검사한 것은 아닙니다.

## 검증의 한계

구조 검사는 모든 PDF와 브라우저에서의 완벽한 시각적 동일성을 보장하지 않습니다. 새 자료마다 시작·중간·완료 프레임을 원본과 비교해야 합니다. 통계·주장의 사실 검증, PowerPoint 네이티브 애니메이션 출력, 외부 호스팅 배포는 이 검사에 포함되지 않습니다.
